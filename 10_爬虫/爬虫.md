##爬虫

###1.爬虫入门

####1.爬虫的基础知识

**1.爬虫的定义和使用场景**

1.1什么是爬虫

```
网络爬虫（又被称为网页蜘蛛，网络机器人）就是模拟浏览器发送网络请求，接收请求响应，一种按照一定的规则，自动地抓取互联网信息的程序。
```

1.2爬虫的用途

```
1.12306抢票
2.网站上的投票
3.短信轰炸
```

**2.爬虫的分类和爬虫的流程**

1.爬虫的分类

```python
1.通用爬虫:通常指搜索引擎的爬虫
2.聚焦爬虫:指针对特定的网站爬虫
```

2.爬虫的流程

```python
向起始的url发送请求并获取响应-->对响应进行提取-->如果提取到url,则继续发送请求获取响应/如果提取到数据,则将数据进行保存
```

3.robots协议

```python
网站通过Robots协议告诉搜索引擎哪些页面可以抓取，哪些页面不能抓取，但它仅仅是互联网中的一般约定
```

**3.Http和https的复习**

1.http和https的区别

```
HTTP
超文本传输协议
默认端口号:80
HTTPS
HTTP + SSL(安全套接字层)，即带有安全套接字层的超本文传输协议
默认端口号：443
HTTPS比HTTP更安全，但是性能更低
```

2.Http请求的过程

![http发送的过程](C:\Users\Jason\Desktop\笔记\biji\爬虫笔记\picture\http发送的过程.png)

3.Http请求的形式 

![http的请求形式](C:\Users\Jason\Desktop\笔记\biji\爬虫笔记\picture\http的请求形式.png)

4.Http常见的请求头

5.重要的响应头

6.响应状态码

**4. 字符**

1.字符集

```
ASCII字符集:1个字节
Unicode字符集:通常是2个字节,特别的像是utf-8是一种可变长的编码方式,可以是1,2,3个字节
GB23123
GB18030
```

2.python3中的字符串

```
str:unicode
bytes:字节类型(一个字节8位,取值范围0-255)
Bytes 对象只负责以二进制字节序列的形式记录所需记录的对象，至于该对象到底表示什么（比如到底是什么字符）则由相应的编码格式解码所决定
```

![不同编码方式下的字节表现形式](C:\Users\Jason\Desktop\笔记\biji\爬虫笔记\picture\不同编码方式下的字节表现形式.png)

3.str和bytes类型的相互转换

```
str --> bytes:str.encode('utf-8')
bytes -->str :bytes.decode('utf-8')
```



#### 2.请求的发送方式

**1.requests模块的基本使用(get)**

1.请求百度图片

```python
import requests
# 目标url
url = 'https://www.baidu.com/img/bd_logo1.png'

#请求头header
headers = {"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/54.0.2840.99 Safari/537.36"}

# 向目标url发送get请求,并带上headers
response = requests.get(url,headers=headers)

with open('baidui.png','wb') as f:
    f.write(response.content)
```

| response             | response.request         |
| -------------------- | ------------------------ |
| response.headers     | response.request.headers |
| response.cookies     | response.request.cookies |
| respones.content     |                          |
| response.text        |                          |
| response.status_code |                          |

2.发送带参数的请求

```python
# 方式一：利用params参数发送带参数的请求
import requests

headers = {"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/54.0.2840.99 Safari/537.36"}


# 最后有没有问号结果都一样
url = 'https://www.baidu.com/s?' 

# 请求参数是一个字典 即wd=python
kw = {'wd': 'python'} 

# 带上请求参数发起请求，获取响应
response = requests.get(url, headers=headers, params=kw) 

# 当有多个请求参数时，requests接收的params参数为多个键值对的字典，比如 '?wd=python&a=c'-->{'wd': 'python', 'a': 'c'}

print(response.content)
```



**2.requests模块的深入使用(post)**

1.使用requests发送POST请求

```python 
response = requests.post("http://www.baidu.com/", \
data = data,headers=headers)
```

2.使用代理

```python
正向代理：对于浏览器知道服务器的真实地址，例如VPN
反向代理：浏览器不知道服务器的真实地址，例如nginx
```

```python
 proxies = { 
      "http": "http://12.34.56.79:9527", 
      "https": "https://12.34.56.79:9527", 
      }
  
 requests.get("http://www.baidu.com",  proxies = proxies)
```

```Python
代理IP的分类:透明代理/匿名代理/高匿代理
反反爬:检查访问频率/购买所有的代理提供商,若检测到是代理测屏蔽掉
代理ip池的更新:
```



**3.requests模块处理cookie相关请求**

```python
# 使用requests处理cookie有三种方法：
1.cookie字符串放在headers中
2.把cookie字典放传给请求方法的cookies参数接收
3.使用requests提供的session模块

```

1.cookie字符串放在headers中

```python
headers = {
"User-Agent":"Mozilla/5.0 (Macintosh; Intel Mac OS X 10_13_4) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/65.0.3325.181 Safari/537.36",
"Cookie":" Pycharm-26c2d973=dbb9b300-2483-478f-9f5a-16ca4580177e; Hm_lvt_98b9d8c2fd6608d564bf2ac2ae642948=1512607763; Pycharm-26c2d974=f645329f-338e-486c-82c2-29e2a0205c74; _xsrf=2|d1a3d8ea|c5b07851cbce048bd5453846445de19d|1522379036"
}

requests.get(url,headers=headers)
```

2.把cookie字典放传给请求方法的cookies参数接收

```python
cookies = {"cookie的name":"cookie的value"}
requests.get(url,headers=headers,cookies=cookie_dict}
```

3.使用requests提供的session模块

```python
session = requests.session()
response = session.get(url,headers)
```



**4.requests模块的其他方法**

1 requests中cookirJar的处理方法

```python
import requests

url = "http://www.baidu.com"
#发送请求，获取resposne
response = requests.get(url)
print(type(response.cookies))
#使用方法从cookiejar中提取数据
cookies = requests.utils.dict_from_cookiejar(response.cookies)
print(cookies)

#注意:
response.cookies是CookieJar类型,使用requests.utils.dict_from_cookiejar，能够实现把cookiejar对象转化为字典
```

2.requests处理证书错误 

```Python
import requests
url = "https://www.12306.cn/mormhweb/"
response = requests.get(url,verify=False)
```

3.超参数的使用

```python
import requests
from retrying import retry

headers = {}

#最大重试3次，3次全部报错，才会报错
@retry(stop_max_attempt_number=3) 
def _parse_url(url)
    #超时的时候回报错并重试
    response = requests.get(url, headers=headers, timeout=3) 
    #状态码不是200，也会报错并重试
    assert response.status_code == 200
    return response


def parse_url(url)
    try: #进行异常捕获
        response = _parse_url(url)
    except Exception as e:
        print(e)
        #报错返回None
        response = None
    return response
```



**5.urlib的学习**

1.urllib的基本方法介绍

```python
#构造headers
headers = {"User-Agent" : "Mozilla/5.0 (compatible; MSIE 9.0; Windows NT 6.1; Trident/5.0)"}
formdata = {
     "type":"AUTO",
     "i":"i love python",
     "doctype":"json",
}
#构造请求
request = urllib.request.Request(url, headers = headers,data = data,)
#发送请求
response = urllib.request.urlopen(request)
print(response.read())
```

```python
#urllib请求百度首页的完整例子
import urllib

url = 'http://www.baidu.com'
#构造headers
headers = {"User-Agent" : "Mozilla/5.0 (compatible; MSIE 9.0; Windows NT 6.1; Trident/5.0)"}
#构造请求
request = urllib.request.Request(url, headers = headers)
#发送请求
response = urllib.request.urlopen(request)
#获取html字符串
html_str = response.read().decode()
print(html_str)
```



####3.数据提取的方法

1.爬虫中的数据分类

```python
1.结构化数据:json,xml等  # 处理方式:直接转化为python数据类型
2.非结构化数据:HTML      # 处理方式:正则表达式,xpath
```

2.json

```python
1.json.loads(json字符串)=python数据类型
2.json.dumps(python) =包含json的类文件对象

1.json.load(包含json的类文件对象)=python的数据类型
2.json.dump(pytohon数据类型)=包含json的类文件对象
```

```python
json_str=json.dumps(mydict,ident=2,ensure_ascil=False)
my_dict =json.loads(json_str)

with open("temp.txt","w") as f:
	json.dumps(mydict,f,ensure_ascii=False,ident=2)
    
with open("temp,txt","r") as f:
    my_dict =json.load(f)

```



3.正则

```python
格式1:re.match("正则表达式","字符串")
格式2:
    p=re.compile("正则表达式") #编译
	p.findall("字符传") 

1.p.match( )#从头找
2.p.search()#找一个
3.p.findall()#存在则返还一个列表,没有则返回一个空列表
4.p.sub()#替换
```

```python
#python中原始字符串r的用法
原始字符串定义(raw string)：所有的字符串都是直接按照字面的意思来使用，没有转义特殊或不能打印的字符，原始字符串往往针对特殊字符而言。例如"\n"的原始字符串就是"\\n"

r"a\nb" == "a\\nb" #True
re.findall("a\nb","a\nb")   #['a\nb']
re.findall("a\\nb","a\nb")  #['a\nb']
re.findall("a\\nb","a\\nb") #[]

re.findall(r"a\nb","a\nb")  #['a\nb']
re.findall(r"a\\nb","a\\nb") #['a\\nb']

注意:
    1.正则中使用原始字符串r能够忽略转义符号带来的影响，加上原始字符串r之后，待匹配的字符串中有多少个\，正则中就添加多少个\即可
	2.re.findall(r“a.*bc”,”a\nbc”,re.DOTALL)和re.findall(r“a(.*)bc”,”a\nbc”,re.DOTALL)的区别？
    不分组时匹配的是全部，分组后匹配的是组内的内容
```



4.xpath

4.1 定义:

```Python
XPath (XML Path Language) 是一门在 HTML\XML 文档中查找信息的语言，可用来在 HTML\XML 文档中对元素和属性进行遍历
```

4.2 xml和html的区别

![认识xml](C:\Users\Jason\Desktop\笔记\biji\爬虫笔记\picture\认识xml.png)

4.3 xpath的节点关系

```
1.根节点:
		parent(父节点)/children(子节点)
		ancestor(父节点)/descent(子节点)
		sibling(同胞节点)
```

4.4 xpath语法

```xml
<bookstore>

<book>
  <title lang="eng">Harry Potter</title>
  <price>29.99</price>
</book>
<book>
  <title lang="eng">Learning XML</title>
  <price>39.95</price>
</book>
    
</bookstore>
```

| 1.选取节点 | 描述                                                       | 举例                | 结果                                             |
| ---------- | ---------------------------------------------------------- | ------------------- | ------------------------------------------------ |
| nodename   | 选中该元素。                                               | bookstore           | 选择bookstore元素                                |
| /          | 从根节点选取、或者是元素和元素间的过渡。                   | /bookstore          | 选取根元素 bookstore                             |
| //         | 从匹配选择的当前节点选择文档中的节点，而不考虑它们的位置。 | //book              | 选取所有 book 子元素，而不管它们在文档中的位置。 |
| .          | 选取当前节点。                                             |                     |                                                  |
| ..         | 选取当前节点的父节点。                                     |                     |                                                  |
| @          | 选取属性。                                                 | //book/title/@lang  | 选择所有的book下面的title中的lang属性的值。      |
| text()     | 选取文本。                                                 | //book/title/text() | 选择所有的book下面的title的文本                  |

| 2.查找特定的节点                    | 结果 |
| ----------------------------------- | ---- |
| //title[@lang="eng"]                |      |
| /bookstore/book[1]                  |      |
| /bookstore/book[last()-1]           |      |
| /bookstore/book[position()>1]       |      |
| //book/title[text()='Harry Potter'] |      |
| /bookstore/book[price>35.00]/title  |      |

| 3.选取未知的节点 | 描述                 |
| ---------------- | -------------------- |
| *                | 匹配任何元素节点。   |
| @*               | 匹配任何属性节点。   |
| node()           | 匹配任何类型的节点。 |

| 4.选取若干路径             | **结果**                                 |
| -------------------------- | ---------------------------------------- |
| //book/title\|//book/price | 选取 book 元素的所有 title 和 price 元素 |



5.IXML模块的学习

*在python爬虫代码中我们如何使用xpath呢,对应的我们需要lxml*

5.1 IXML的基本使用

```python
from lxml import etree  #导入lxml 的 etree 库 (导入没有提示不代表不能用)
html = etree.HTML(text) #将字符串转化为Element对象
ret_list = html.xpath("xpath字符串") #返还xpath查询结果后的列表
handeled_html_str=etree.tostring(ret_list).decode() #将Element对象转换为字符串,返还bytes类型
print(handeled_html_str)
```

5.2 IXML的深入使用

```
1.接下来我们继续操作，假设每个class为item-1的li标签是1条新闻数据，如何把这条新闻数据组成一个字典
2.前面我们取到属性，或者是文本的时候，返回字符串 但是如果我们取到的是一个节点，返回什么呢?
答:
返回的是element对象，可以继续使用xpath方法，对此我们可以在后面的数据提取过程中：先根据某个标签进行分组，分组之后再进行数据的提取
```

```python
from lxml import etree
text = ''' <div> <ul> 
        <li class="item-1"><a>first item</a></li> 
        <li class="item-1"><a href="link2.html">second item</a></li> 
        <li class="item-inactive"><a href="link3.html">third item</a></li> 
        <li class="item-1"><a href="link4.html">fourth item</a></li> 
        <li class="item-0"><a href="link5.html">fifth item</a> 
        </ul> </div> '''

#根据li标签进行分组
html = etree.HTML(text)
li_list = html.xpath("//li[@class='item-1']")

#在每一组中继续进行数据的提取
for li in li_list:
    item = {}
    item["href"] = li.xpath("./a/@href")[0] if len(li.xpath("./a/@href"))>0 else None
    item["title"] = li.xpath("./a/text()")[0] if len(li.xpath("./a/text()"))>0 else None
    print(item)
```



6.BeautifulSoup4

6.1 css选择器BeautifulSoup4

```python
#pip install beautifulsoup4
from bs4 import BeautifulSoup

html = """
<html><head><title>The Dormouse's story</title></head>
<body>
<p class="title" name="dromouse"><b>The Dormouse's story</b></p>
<p class="story">Once upon a time there were three little sisters; and their names were
<a href="http://example.com/elsie" class="sister" id="link1"><!-- Elsie --></a>,
<a href="http://example.com/lacie" class="sister" id="link2">Lacie</a> and
<a href="http://example.com/tillie" class="sister" id="link3">Tillie</a>;
and they lived at the bottom of a well.</p>
<p class="story">...</p>
"""


soup = BeautifulSoup(html)               #创建 Beautiful Soup 对象
soup = BeautifulSoup(open('index.html'),"lxml") #打开本地 HTML 文件的方式来创建对象
print soup.prettify()                    #格式化输出 soup 对象的内容
```

6.2 搜索文档树

6.2.1 find_all/find

```python
#传字符串
print (soup.find_all('a'))
#传正则表达式
soup.find_all(re.compile("^b"))
#传列表
soup.find_all(["a", "b"])
soup.find_all(class = "sister")
soup.find_all(text="Elsie")


注意:find返回第一个符合匹配结果,find_all则返回所有匹配结果的列表
```

6.2.2 css选择器

```python
#1.通过标签选择器查找
soup.select('title')
#2.通过类选择器查找
soup.select('.sister')
#3.通过 id 选择器查找
soup.select('#link1')
#4.层级选择器查找
soup.select('p #link1')
#5.通过属性选择器查找
soup.select('a[class="sister"]')
#6.获取文本内容 get_text()
soup.select('title')[0].get_text()
#7.获取属性 get('属性的名字')
 soup.select('a')[0].get('href')
    
注意:
    find 方法返回一个解析完毕的对象
    fid_all/select 方法返回的结果都是列表形式
```

### 2.爬虫提高

####1.高性能爬虫

1.单线程

```python
import requests
from lxml import etree
import time

class QiuBai:
    def __init__(self):
        self.temp_url = "http://www.qiushibaike.com/8hr/page/{}"
        self.headers = {"User-Agent": "Mozilla/5.0 (Macintosh; Intel Mac OS X 10_13_4) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/66.0.3359.139 Safari/537.36"}

    def get_url_list(self):#准备url地址的刘表
        return [self.temp_url.format(i) for i in range(1,14)]

    def parse_url(self,url):#发送请求，获取响应
        response = requests.get(url,headers=self.headers)
        return response.content.decode()

    def get_content_list(self,html_str):#提取数据
        html = etree.HTML(html_str)
        div_list = html.xpath("//div[@id='content-left']/div")
        content_list = []
        for div in div_list:
            item = {}
            item["user_name"] = div.xpath(".//h2/text()")[0].strip()
            item["content"] = [i.strip() for i in div.xpath(".//div[@class='content']/span/text()")]
            content_list.append(item)
        return content_list

    def save_content_list(self,content_list):  '''保存'''
        for content in content_list:
            print(content)
            
    def run(self):#实现做主要逻辑
        url_list = self.get_url_list() #1.准备url列表
        for url in url_list:#2.遍历发送请求，获取响应
            html_str = self.parse_url(url) #3.提取数据
            content_list = self.get_content_list(html_str) #4. 保存
            self.save_content_list(content_list)


if __name__ == '__main__':
    t1 = time.time()
    qiubai = QiuBai()
    qiubai.run()
    print("total cost:",time.time()-t1)
```

2.多线程

```python
import requests
from lxml import etree
from queue import Queue
import threading


class Qiubai:
    def __init__(self):
        self.temp_url = "https://www.qiushibaike.com/8hr/page/{}/"
        self.headers= {"User-Agent":"Mozilla/5.0 (Macintosh; Intel Mac OS X \
        10_13_3) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/64.0.3282.186 Safari/537.36"}
        self.url_queue = Queue()
        self.html_queue = Queue()
        self.content_list_queue = Queue()

    def get_url_list(self):#获取url列表
        for i in range(1,14):
            self.url_queue.put(self.temp_url.format(i))

    def parse_url(self):
        while True: 
            url = self.url_queue.get()
            response = requests.get(url,headers=self.headers)
            self.html_queue.put(response.content.decode())
            self.url_queue.task_done()


    def get_content_list(self):  #提取数据
        while True:
            html_str = self.html_queue.get()
            html = etree.HTML(html_str)
            div_list = html.xpath("//div[@id='content-left']/div")
            content_list = []
            for div in div_list:
                content = {}
                content["content"] = div.xpath(".//div[@class='content']/span/text()")
                content_list.append(content)
            self.content_list_queue.put(content_list)
            self.html_queue.task_done()

    def save_content_list(self):
        while True:
            content_list = self.content_list_queue.get()
            pass
            self.content_list_queue.task_done()

    def run(self):
        thread_list = []
        t_url = threading.Thread(target=self.get_url_list) 
        thread_list.append(t_url)
        
        for i in range(3):  
            t_parse = threading.Thread(target=self.parse_url)
            thread_list.append(t_parse)
        
        t_content = threading.Thread(target=self.get_content_list)
        thread_list.append(t_content)
       
        t_save = threading.Thread(target=self.save_content_list)
        thread_list.append(t_save)

        for t in thread_list:
            t.setDaemon(True)  
            t.start()

        for q in [self.url_queue,self.html_queue,self.content_list_queue]:
            q.join() 
        print("主线程结束")

if __name__ == '__main__':
    qiubai = Qiubai()
    qiubai.run()
```



3.多进程爬虫

```python
import requests
from lxml import etree
import threading
from multiprocessing import Process
from multiprocessing import JoinableQueue as Queue

class QiubaiSpider:
    def __init__(self):
        self.url_temp = "https://www.qiushibaike.com/8hr/page/{}/"
        self.headers = {"User-Agent":"Mozilla/5.0 (Macintosh; Intel Mac OS X 10_13_3) \
        AppleWebKit/537.36 (KHTML, like Gecko) Chrome/64.0.3282.186 Safari/537.36"}
        self.url_queue = Queue()  #保存url
        self.html_queue = Queue() #保存html字符串
        self.content_queue = Queue() #保存提取到的数据

    def get_url_list(self):
        for i in range(1,14):
            self.url_queue.put(self.url_temp.format(i))

    def parse_url(self):
        while True:
            url = self.url_queue.get()
            print(url)
            html_str =  requests.get(url,headers=self.headers).content.decode()
            self.html_queue.put(html_str)
            self.url_queue.task_done()

    def get_content_list(self):
        while True:
            html_str = self.html_queue.get()
            html = etree.HTML(html_str)
            div_list = html.xpath("//div[@id='content-left']/div")
            content_list = []
            for div in div_list:
                content = {}
                content["content"]=div.xpath(".//div[@class='content']/span/text()")
                content_list.append(content)
            self.content_queue.put(content_list)
            self.html_queue.task_done()

    def save_content_list(self):
        while True:
            content_list = self.content_queue.get()
            pass
            self.content_queue.task_done()

    def run(self):
        process_list = []
       
        t_url = Process(target=self.get_url_list)
        process_list.append(t_url)
        
        for i in range(5):
            t_parse = Process(target=self.parse_url)
            process_list.append(t_parse)
        
        t_content = Process(target=self.get_content_list)
        process_list.append(t_content)
       
        t_save = Process(target=self.save_content_list)
        process_list.append(t_save)

        for t in process_list:
            t.daemon=True 
            t.start()

        for q in [self.url_queue,self.html_queue,self.content_queue]:
            q.join() 

        print("主进程结束")

if __name__ == '__main__':
    qiubai = QiubaiSpider()
    qiubai.run()
```



4.线程池实现爬虫

```python
import requests
from lxml import etree
from queue import Queue
from multiprocessing.dummy import Pool
import time


class QiubaiSpider:
    def __init__(self):
        self.url_temp = "https://www.qiushibaike.com/8hr/page/{}/"
        self.headers = {"User-Agent": "Mozilla/5.0 (Macintosh; Intel Mac OS X \
        10_13_3) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/64.0.3282.186 Safari/537.36"}
        self.queue = Queue()
        self.pool = Pool(5)
        self.is_running = True
        self.total_requests_num = 0
        self.total_response_num = 0

    def get_url_list(self):  # 获取url列表
        for i in range(1, 14):
            self.queue.put(self.url_temp.format(i))
            self.total_requests_num += 1

    def parse_url(self, url):  # 发送请求，获取响应
        return requests.get(url, headers=self.headers).content.decode()

    def get_content_list(self, html_str):  # 提取段子
        html = etree.HTML(html_str)
        div_list = html.xpath("//div[@id='content-left']/div")
        content_list = []
        for div in div_list:
            content = {}
            content["content"] = div.xpath(".//div[@class='content']/span/text()")
            print(content)
            content_list.append(content)
        return content_list

    def save_content_list(self, content_list):  # 保存数据
        pass

    def exetute_requests_item_save(self):
        url = self.queue.get()
        html_str = self.parse_url(url)
        content_list = self.get_content_list(html_str)
        self.save_content_list(content_list)
        self.total_response_num += 1

    def _callback(self, temp):
        if self.is_running:
            self.pool.apply_async(self.exetute_requests_item_save, callback=self._callback)

    def run(self):
        self.get_url_list()

        for i in range(2):  # 控制并发
            self.pool.apply_async(self.exetute_requests_item_save, callback=self._callback)

        while True:  # 防止主线程结束
            time.sleep(0.0001)  # 避免cpu空转，浪费资源
            if self.total_response_num >= self.total_requests_num:
                self.is_running = False
                break


if __name__ == '__main__':
    qiubai = QiubaiSpider()
    qiubai.run()
```

5.使用协程池进行爬虫

```python
import gevent.monkey
gevent.monkey.path_all()
from gevent.pool import Pool


import requests
from lxml import etree
from queue import Queue
import time


class QiubaiSpider:
    def __init__(self):
        self.url_temp = "https://www.qiushibaike.com/8hr/page/{}/"
        self.headers = {"User-Agent": "Mozilla/5.0 (Macintosh; Intel Mac OS X \
        10_13_3) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/64.0.3282.186 Safari/537.36"}
        self.queue = Queue()
        self.pool = Pool(5)
        self.is_running = True
        self.total_requests_num = 0
        self.total_response_num = 0

    def get_url_list(self):  # 获取url列表
        for i in range(1, 14):
            self.queue.put(self.url_temp.format(i))
            self.total_requests_num += 1

    def parse_url(self, url):  # 发送请求，获取响应
        return requests.get(url, headers=self.headers).content.decode()

    def get_content_list(self, html_str):  # 提取段子
        html = etree.HTML(html_str)
        div_list = html.xpath("//div[@id='content-left']/div")
        content_list = []
        for div in div_list:
            content = {}
            content["content"] = div.xpath(".//div[@class='content']/span/text()")
            content_list.append(content)
        return content_list

    def save_content_list(self, content_list):  # 保存数据
        pass

    def exetute_requests_item_save(self):
        url = self.queue.get()
        html_str = self.parse_url(url)
        content_list = self.get_content_list(html_str)
        self.save_content_list(content_list)
        self.total_response_num += 1

    def _callback(self, temp):
        if self.is_running:
            self.pool.apply_async(self.exetute_requests_item_save, callback=self._callback)

    def run(self):
        self.get_url_list()
        for i in range(2):  # 控制并发
            self.pool.apply_async(self.exetute_requests_item_save, callback=self._callback)

        while True:  # 防止主线程结束
            time.sleep(0.0001)  # 避免cpu空转，浪费资源
            if self.total_response_num >= self.total_requests_num:
                self.is_running = False
                break


if __name__ == '__main__':
    qiubai = QiubaiSpider()
    qiubai.run()
```

```python
1.爬取百度贴吧
2.爬取果壳以及36kr
```

#### 2.selenium

```
1.Selenium是一个Web的自动化测试工具它支持所有主流的浏览器
可以接收指令，让浏览器自动加载页面，获取需要的数据，甚至页面截屏
```

2.1selenium的基本使用

```python
import time
from selenium import webdriver
driver = webdriver.Chrome()
driver.get("http://www.baidu.com/")
driver.find_element_by_id("kw").send_keys("长城")
driver.find_element_by_id("su").click()
time.sleep(3)
driver.save_screenshot("baidu.png")

print(driver.page_source)
print(driver.get_cookies()) 
print(driver.current_url) #获取请求的url信息
time.sleep(3)
driver.close() #退出当前页面
driver.quit()  #退出浏览器
```

2.2 selenium的

```python
from selenium import webdriver

driver = webdriver.Chrome()
driver.get("https://www.douban.com/")

ret1 = driver.find_elements_by_xpath("//*[@id='anony-nav']/h1/a") #返回一个包含元素的列表

ret2 = driver.find_elements_by_class_name("app-title") #根据类名获取元素列表

ret3= driver.find_element_by_id("anony-nav") #返回一个元素

ret4 = driver.find_elements_by_tag_name("h1") #根据标签名获取元素列表

ret5= driver.find_elements_by_link_text("下载豆瓣 App") #根据连接文本获取元素列表

ret6 = driver.find_elements_by_partial_link_text("豆瓣")#根据链接包含的文本获取元素列表

print(ret1)
print(ret2)
print(ret3)
print(ret4)
print(ret4[0].text)#获取文本
print(ret5)
print(ret5[0].get_attribute("href"))#获取属性值
print(ret6)
```

2.3selennium的其他方法

2.3.1selenium 处理cookie

```python
driver.get_cookies() #获取所有的cookie
driver.delete_cookie("CookieName") #删除一条cookie
driver.delete_all_cookies() #删除所有的cookie

```

2.3.2 页面等待

```python
time.sleep(10)
```

2.3.3 switch方法切换的操作

```python
current_windows = driver.window_handles #获取当前所有的窗口
driver.switch_to.window(current_windows[1]) #根据窗口索引进行切换

driver.switch_to.frame()
# iframe是html中常用的一种技术，即一个页面中嵌套了另一个网页，selenium默认是访问不了frame中的内容的

alert = driver.switch_to_alert() #处理弹窗和提示的方法

driver.forward()     #前进

driver.back()        # 后退
```

2.3.4 作业

```python
1.爬取斗鱼直播平台的所有房间信息
2.爬取网易云音乐的所有分类下的所有的播放列表,包括：播放列表的标题和url地址
```

#### 3.反扒及解决方案

3.1常见的反扒手段

```python
1.明确反反爬的主要思路:尽可能的去模拟浏览器，浏览器在如何操作，代码中就如何去实现

2.通过headers字段来反爬
	2.1 通过headers中的User-Agent字段来反爬
	2.2通过referer字段或者是其他字段来反爬(添加referer字段 例如豆瓣电视剧中)
	2.3通过cookie来反爬

3.通过js来反爬
	3.1 通过js实现跳转来反爬(在chrome中点击perserve log按钮实现观察页面跳转情况)
    3.2 通过js生成了请求参数(分析js，观察加密的实现过程，通过js2py获取js的执行结果，或者使用selenium来实现)
    3.3 通过js实现了数据的加密(分析js，观察加密的实现过程，通过js2py获取js的执行结果，或者使用selenium来实现)

4.通过验证码来反爬(打码平台或者是机器学习的方法识别验证码)
5 通过ip地址来反爬(购买高质量的ip的方式能够解决问题)
6.通过用户行为来反扒(通过获取数据的情况来观察请求，寻找异常出现的可能请求)
7.通过自定义字体来反爬(切换到手机版)
8.通过css来反爬(计算css的偏移)
```

3.2打码平台的使用

```python
#常见的验证码的种类
1.url地址不变，验证码不变

2.url地址不变，验证码变化
(提交验证码的到时候需要保证cookie的一致性，对此可以使用requests.session来解决)
```

```python
from selenium import webdriver
import time
import  requests
from yundama import indetify

driver = webdriver.Chrome()

driver.get("https://www.douban.com/")

#输入用户名
driver.find_element_by_id("form_email").send_keys("78****@qq.com")

#输入密码
driver.find_element_by_id("form_password").send_keys("****")

#获取验证码的地址
img_url = driver.find_element_by_id("captcha_image").get_attribute("src")
response = requests.get(img_url)  #请求验证码的地址
ret = indetify(response.content)  #验证码识别

#输入验证码
driver.find_element_by_id("captcha_field").send_keys(ret)


time.sleep(5)
#点击登录
driver.find_element_by_class_name("bn-submit").click()


time.sleep(10)
print(driver.get_cookies())
driver.quit()
```

3.3 谷歌浏览器的使用

```python
1 新建隐身窗口
2 preserve log (保存日志)
3.filter过滤
4.XHR:大部分情况表示ajax请求/JS:js请求/CSS:css请求
5.寻找登录接口
	5.1 form表单中action对应的url地址
    5.2 通过抓包寻找登录的url地址/切换到手机登录版本
```

4.JS解析

```python
1.确定JS的位置
    1.1 观察按钮的绑定js事件
    1.2 通过search all file 来搜索
    
2.观察js的执行过程
	1.添加断点
    找到js具体在如何执行后,python程序模拟js的执行/JS2py直接把js代码转化为python程序去执行

3.js2py的使用
     需要准备js的内容
	 生成js的执行环境
	 在执行环境中执行js的字符串，传入数据，获取结果   
```

#### 4.Mongodb

4.1MongoDB的介绍和安装

```python
#mongodb的优势
1.易扩展: 数据之间无关系,非常容易扩展
2.大数据量:具有非常高的读写性能
3.灵活的数据模:NoSQL无需事先为要存储的数据建立字段
```

4.2MongoDB超级管理员账号的创建

```python
sudo mongod -auth #以权限认证的方式启动mongodb数据库
use admin     #使用admin数据库
db.createUser({"user":"python","pwd":"python","roles":["root"]})   #创建超级用户

use admin
db.auth('python','python')
```

```python
use test1    #创建用户的数据库
db.createUser("user":"user1", "pwd":"pwd1", roles:["read"])		#创建普通用户
db.createUser("user":"user1", "pwd":"pwd1", roles:["readWrite"])

use admin   #在其他数据库上创建普通用户
db.createUser({"user":"python1", "pwd":"python1", roles:[{"role":"read","db":"dbname1"},{"role":"readWrite","db":"dbname2"}
]})
```

```python
show users
db.dropUser('python')
```

4.3MongoDB的入门使用

| MongoDB数据库的命令 | 含义             |
| ------------------- | ---------------- |
| db                  | 查看当前的数据库 |
| show dbs            | 查看所有的数据库 |
| use db_name         | 切换数据库       |
| db.dropDatabase()   | 删除当前的数据库 |

| 集合                                                        | 含义     |
| ----------------------------------------------------------- | -------- |
| `db.createCollection(name,options)`                         | 创建集合 |
| `db.createCollection("stu")`                                |          |
| `db.createCollection("sub", { capped : true, size : 10 } )` |          |
| `show collections`                                          | 查看集合 |
| `db.stu.drop()`                                             | 删除集合 |

| 常见数据类型 | 1                  |
| ------------ | ------------------ |
| Object ID    | ⽂档ID             |
| String       | 字符串             |
| Boolean      | 布尔值             |
| Integer      | 整数               |
| Double       | 浮点值             |
| Arrays       | 数组               |
| Object       | ⽤于嵌⼊式的⽂档   |
| Timestamp    | 时间戳             |
| Date         | 存储当前⽇期或时间 |

| mongodb的增删改查                                    | 含义                                                     |
| ---------------------------------------------------- | -------------------------------------------------------- |
| `db.stu.insert({_id:"20170101",name:'gj',gender:1})` | 如果不指定_id参数,MongoDB会为⽂档分配⼀个唯⼀的 ObjectId |
| `db.stu.remove( <query>,{justOne: <boolean>} )`      | 删除                                                     |
| `db.stu.update({},{$set:{gender:0}},{multi:true})`   | 更新全部                                                 |
| `db.stu.find({条件⽂档})`                            | 简单查询                                                 |
| `db.stu.findOne({条件⽂档})`                         | 查询，只返回第⼀个                                       |
| `db.stu.find({条件⽂档}).pretty()`                   | 将结果格式化                                             |

| 比较运算 | 1                              |
| -------- | ------------------------------ |
| `$lt`    | `db.stu.find({age:{$gte:18}})` |
| `$lte`   | `db.stu.find({age:{$lte:18}})` |
| `$gt`    | `db.stu.find({age:{$gt:18}})`  |
| `$gte`   | `db.stu.find({age:{$gte:18}})` |
| `$ne`    | `db.stu.find({age:{$nt:18}})`  |

| 运算       |              |                                                              |
| ---------- | ------------ | ------------------------------------------------------------ |
| 逻辑运算   | `and`        | `db.stu.find({age:{$gte18},gender:ture})`                    |
|            | `or`         | `db.stu.find({   $or:[  {age:{$gt:18}},{gender:false } ]  })` |
| 范围运算   | `$in/$nin`   | `db.stu.find({age:{$in:[18,28,38]}})`                        |
| 正则表达式 | `//`         | `db.products.find({sku:/^abc/})`                             |
|            | `$regex`     | `db.products.find({ sku:{ $ regex:" 789 $ "} })`             |
| 自定义查询 | `$where`     | `db.stu.find({  $where:function(){ return this.age>30;}  })` |
| 投影       |              | `db.stu.find({},{_id:0,name:1,gender:1})`                    |
| 排序       | `sort`       | `db.stu.find().sort({gender:-1,age:1})`                      |
| 统计个数   | `count()`    | `db.stu.find({gender:true}).count()                db.stu.count({age:{$gt:20},gender:true})` |
| 消除重复   | `distinct()` | `db.stu.distinct('hometown',{age:{$gt:18}})`                 |

4.4mongodb的聚合操作

| 常用管道命令 | 1 ( db.stu.aggregate({ }) )                                  | 2                                                            | 3                                                            |
| ------------ | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `$group`     | `db.stu.aggregate({            $group:{ _id:"$gender",                           counter:{$sum:1}         }                              })` | `db.stu.aggregate(     {$group:         {             _id:null,             counter:{$sum:1}         }     }` | `db.stu.aggregate(      {$group:          {              _id:null,              name:{$push:"$$ROOT"}          }      }  )` |
| `$match`     | `db.stu.aggregate(      {$match:{age:{$gt:20}}      )`       | ` db.stu.aggregate(      {$match:{age:{$gt:20}}      {$group:{_id:"$gender",counter:{$sum:1}}}      )` |                                                              |
| `$project`   | ` db.stu.aggregate(      {$project:{_id:0,name:1,age:1}}      )` | ` db.stu.aggregate(      {$group:{_id:"$gender",counter:{$sum:1}}}      {$project:{_id:0,counter:1}}      )` |                                                              |
| `$sort`      | `db.stu.aggregate({$sort:{age:1}})`                          | ` db.stu.aggregate(      {$group:{_id:"$gender",counter:{$sum:1}}},      {$sort:{counter:-1}}  )` |                                                              |
| `$limit`     | `db.stu.aggregate(      {$limit:2}  )`                       |                                                              |                                                              |
| `$skip`      | ` db.stu.aggregate(      {$skip:3}  )`                       |                                                              |                                                              |
| `$sum`       | 计算总和， $sum:1 表示以⼀倍计数                             |                                                              |                                                              |
| `$avg`       | 计算平均值                                                   |                                                              |                                                              |
| `$min`       | 获取最⼩值                                                   |                                                              |                                                              |



2.4.5Mongodb的索引

| 1            | 2                                               | 3                              |
| ------------ | ----------------------------------------------- | ------------------------------ |
| 创建索引     | `db.stu.ensureIndex({属性:-1},{"unique":true})` | `db.stu.createIndex({属性:1})` |
| 查看索引     | `db.stu.getIndexes()`                           |                                |
| 删除索引     | `db.stu.drop({'索引名称':1})`                   |                                |
| 建立符合索引 | `db.stu.ensureIndex({字段1:1,字段2:1})`         |                                |
|              | 数据量大且频繁查询则创建索引                    |                                |



2.4.6mongodb的备份恢复与导出和导入

```python
#备份
mongodump -h 192.168.196.128:27017 -d 数据库名 -o ./backup
#恢复
mongorestore -h 192.168.196.128:27017 -d 数据库名 --dir ./backup
#导出
mongoexport -h 192.168.196.128:27017 -d test -c stu -o test_stu_backup [--type csv -f name,age,number]
#导入
mongoimport -h 192.168.196.128:27017 -d test -c stu -o test_stu_backup [--type csv -f name,age,number]
    
3306.6379,27017
```



2.4.7 mongodb 和python交互

```python
# pip install pymongo
from pymongo import MongoClient
client =MongoClient('127.0.0.1',27017)
collection =collection[test][stu]
#插入数据
ret=collection.insert_one({"name":"Jason01","age":33})
print(ret)
item_list=[{'name':'json0{}'.format(i)} for i in range(10)]
t=collection.insert_many(item_list)
#查询数据
t=collection.find_one({"name":"Jason01"})
for i in t:
    print(i)
#更新
collection.update({"name":"Jason01"},{'name':"new_Json01",multi=True/False})
collection.update({"naem":"Jason哦"},{"$set":{"name":"new_Jason01"}},multi=True/False)
collection.updae_one({"name":'Jason01'},{'$set':{"name":'new_Jason01'}})
collection.update_many({"name":'Jason01'},{'$set':{"name":'new_Jason01'}})


#删除
collection.delete_one({"name":'Jason1'})
collection.delete_many({"name":"test10010"})
```

####5.scrapy框架

5.1scrapy的基础概念和流程

5.1.1概念

```python
#什么是scrapy
scrapy使用了twisted异步网络框架,并且能过提取结构性数据
#异步和非阻塞之间的却别
异步关注的是过程,不管有无结果调用都会返回
非阻塞关注的是等待调用时的状态,在得到这个调用结果之前,该调用不会阻塞当前线程
```

5.1.2scrapy的工作流程

![爬虫流程-1](C:\Users\Jason\Desktop\笔记\note\爬虫笔记\picture\爬虫流程-1.png)



![爬虫流程-3](C:\Users\Jason\Desktop\笔记\note\爬虫笔记\picture\爬虫流程-3.png)

```python
1.Scheduler-->Engine-->Downloader Middleware-->Downloader
2.Downloader-->Downloader Middleware-->Engine-->spider Middleware -->spider
3.spider(url)-->Spider Middleware-->Engine-->Scheduler
4.spider(data)-->Pipeline
```

2.5.2 scrapy 的入门使用

```python
#创建scrapy项目
scrapy startproject mySpider
cd MySpider
#创建爬虫
scrapy genspider itcast itcast.cn
#完善spider
cd /myspider/myspider/spiders/itcast.py
#修改pipelines.py

#在setting文件中开启pipeline/设置robots协议/设置User-Agent：
ITEM_PIPELINES = {
    'myspider.pipelines.ItcastPipeline': 400
}
ROBOTSTXT_OBEY = False

USER_AGENT = 'Mozilla/5.0 (Macintosh; Intel Mac OS X 10_12_5) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/59.0.3071.115 Safari/537.36'

#运行爬虫
scrapy crawl itcast
```

2.5.3 scrapy发送翻页请求

```python
#1.scrapy实现翻页请求
1.
确定url地址
2.
构造请求:scrapy.Requests(url,callback)
    	  yield.scrapy.Request(url,callback)
3.
scrapy.Request(url[,callback,method="GET",headers,body,cookies,\meta,dont_filter=False])

```

```python
#2.item的使用
定义item即提前规划好哪些字段需要抓取

class Myspider1Item(scrapy.Item):
    # define the fields for your item here like:
    name = scrapy.Field()
    level=scrapy.Field()
    text=scrapy.Field()
```

2.5.4scrapy的深入使用

```python
#1.debug的信息

#2.scrapy shell命令
scrapy shell http://www.itcast.cn/channel/teacher.shtml
response.xpath()：直接测试xpath规则是否正确

#3.settings.py中的设置信息
USER_AGENT
ROBOTSTXT_OBEY
CONCURRENT_REQUESTS
DOWNLOAD_DELAY 
COOKIES_ENABLED 
DEFAULT_REQUEST_HEADERS
SPIDER_MIDDLEWARES
DOWNLOADER_MIDDLEWARES 
LOG_LEVEL = "WARNING"
LOG_FILE = "./test.log"

#4.pipeline管道的使用
1.process_item(self,item,spider):实现对item数据的处理
2.open_spider(self, spider): 在爬虫开启的时候仅执行一次
3.close_spider(self, spider): 在爬虫关闭的时候仅执行一次
```

2.5.5crawlspider类的使用

```python
#为什么使用crawlspider,crawlspider是什么
1.为了方便我们寻找下一页的url地址或者是内容的url地址
2.从response中提取所有满足规则的url地址/自动构造自己的requests请求,发送给引擎


#创建crawlspider爬虫
scrapy genspider -t crawl itcast itcast.cn
#url规则
rules = (
        Rule(LinkExtractor(allow=r'Items/'), callback='parse_item', follow=True),
)
#LinkExtractor(链接提取器)参数
allow/deny/allow_domains/deny_domains/retrict_xpath

#Rule常见参数
LinxExtracor:链接提取器
callback:提取到的url地址响应的回调函数
follow:链接提取其中的url地址是否还会被rules中的规则继续提取
process_links:这个方法执行后才会执行callback指定的方法,这个方法可以用来过滤url

```

2.5.6 scrapy中间件

```python
#1.种类
1.下载中间件
2.爬虫中间件
#2.scrapy 中间件的作用
1.如对非200响应的重试
2.对header以及cookie进行更换
3.根据其他业务需求实现响应功能
#3.实现随机User-Agent的下载中间件
class UserAgentMiddleware(object):
    def process_request(self, request, spider):
        user_agent = random.choice(USER_AGENTS_LIST)
        request.headers['User-Agent'] = user_agent
#4.代理ip的使用
class ProxyMiddleware(object):
    #添加代理ip
    def process_request(self,request,spider):
        proxy = random.choice(proxies)
        request.meta['proxy'] = proxy
        return None 
   def process_response(self, request, response, spider):
    	#检测代理ip是否可用
        if response.status != '200' and response.status != '302':
            #此时对代理ip进行操作，比如删除
            return request
    
    

```

2.5.7scrapy模拟登陆

```python
#requests模块如何实现模拟登陆
1.直接携带cookies请求页面
2.找url地址，发送post请求存储cookie
#selenium是如何模拟登陆
1.找到对应的input标签，输入文本点击登陆
#scrapy有三种模登陆
1.直接携带cookies
2.找url地址，发送post请求存储cookie
3.找到对应的form表单，自动解析input标签，自动解析post请求的url地址，自动带上数据，自动发送请求
```

```python
#scrapy携带cookies直接获取需要登陆后的页面
1.重构scrapy的start_requests方法,并在其中手动添加cookies
2.携带cookie登陆github

#scrapy.FormRequest发送post请求
1.scrapy.Request(method='post',body)
2.scrapy.FormRequest(ip地址,formdata={},callback=解析函数)

#scrapy自动提交表单(它能够自动的从响应中寻找form表单，然后把formdata中的数据提交到action对应的url地址中)
1.scrapy.Formrequest.from_rsponse(response,formxpath='xpath语句',formdata={},callback=回调函数)
2在settings.py中通过设置COOKIES_DEBUG=TRUE()

```

####6.scrapy_redis

```python
1.请求对象的持久化
2.去重的持久化
3.和实现的持久化
```

2.6.1 scrapy_redis分布式原理

| list                   | set                  | zset                           |
| ---------------------- | -------------------- | ------------------------------ |
| `lpush mylist "world"` | `sadd myset "hello"` | `zadd myset  1 "one"  2 "two"` |
| `lrange mylist 0 -1`   | `smembers myset`     | `zrange myset 0 -1 withscores` |
| `llen mylist`          | `scard myset`        | `zcard myset`                  |

2.6.2 scrapy_redis domz 爬虫分析

```python

```

2.6.3scrapy_redis实现分布式爬虫

```python
#RedisSpider
1.1 分析demo中的代码
1.2 动手实现当当图书爬虫
#RedisCrawlSpider
2.1分析demo中的代码
2.2动手完成亚马逊图书的爬虫

```

####7.爬虫的部署

7.1scrapyd的使用

```python
#1.scrapyd的介绍
scrapy允许你通过Json API 来部署爬虫项目和控制爬虫运行
#2.scrapyd安装
pip3 install scrapyd
pip  install scrapyd-client

#3.启动scrapyd服务
sudo scrapyd (6800)

#4.scrapy项目的部署
1.配置需要部署的项目
[deploy:部署名(部署名可以自行定义)]
url = http://localhost:6800/
project = 项目名(创建爬虫项目时使用的名称)

2.部署项目到scrapyd
scrapyd-deploy -p 项目名称

启动项目：
curl http://localhost:6800/schedule.json -d project=project_name -d spider=spider_name

关闭爬虫：
curl http://localhost:6800/cancel.json -d project=project_name -d job=jobid
```

7.2pycharm通过sftp发布代码

```python
#1.Tools/deployment/Configuration
#2.加号+起个名字+sftp
#3.
sftp hosts中写入服务的ip
并点击Test SFTP connection进行测试连接
在Root path中选择服务器中代码部署的路径
#4.
点击Mappings选项
分别选择本地项目代码的路径和目标服务器代码的具体路径后点击OK
#5.
```

7.3 crontab定时任务

```python
#1.crontab的安装和介绍
apt-get install cron
cron -e 进入编辑页面
crontab -l 查看当前定时任务

#2.crontab在爬虫中使用
2.1 使用流程
	把爬虫启动命令写入sh文件
	给sh脚本添加可执行权限
	编辑crontab的配置文件
    
2.2 myspier.sh例子

cd `dirname $0` || exit 1
python ./main.py >> run.log 2>&1

#3.添加可执行权限
sudo chmod +x myspder.sh
crontab -e
 0 6 * * * /home/ubuntu/..../myspider.sh >> /home/ubuntu/.../run2.log 2>&1
```



### 扩充

```python 
1.安装git:sudo apt-get install git
2.安装gitee插件 :setting/plugins/gitee
			   vcsCheckout from Version Control下有Gitee
参考链接:https://blog.csdn.net/sui_yi123/article/details/82777811
3.设置公钥:
		3.1 cat ~/.ssh/id_rsa.pub
		3.2 rm -rf id_rsa id_rsa.pub
		3.3 ssh -keygen -t rsa(存在问题)
		3.4 ssh -T git@gitee.com
		问题:git添加公钥后报错sign_and_send_pubkey: signing failed: agent refused operation的			解决办法
        1.eval "$(ssh-agent -s)"
        2.ssh-add
        参考链接:https://gitee.com/help/articles/4181#article-header0
3. git config user.name "XiaoPawnYeNo1"
   git config user.email "XiaoPawnYeNo1@qq.com"
```

```
问题:
/usr/bin/python: Error while finding spec for 'virtualenvwrapper.hook_loader' (ImportError: No module named 'virtualenvwrapper')
virtualenvwrapper.sh: There was a problem running the initialization hooks. 

If Python could not import the module virtualenvwrapper.hook_loader,
check that virtualenvwrapper has been installed for
VIRTUALENVWRAPPER_PYTHON=/usr/bin/python and that PATH is
set properly
1.安装虚拟环境

```

day01

1.http和https的区别

```
1.80端口443端口
2.超文本传输协议 /超文本安全协议
3.ca证书
4.https相比于http会慢些
```

2.get和Post的区别

```
1.post中才有请求体get没有
2.常见的状态码

```

| 请求方式       | get                            | post                                                         |
| -------------- | ------------------------------ | ------------------------------------------------------------ |
| url可见性      | 参数url可见                    | url参数不可见                                                |
| 数据传输       | 通过拼接url进行传递参数        | 通过body体传输参数                                           |
| 缓存           | get请求是可以缓存的            | post请求不可以缓存                                           |
| 后退页面的反应 | get请求页面后退时，不产生影响  | post请求页面后退时，会重新提交请求                           |
| 传输数据的大小 | get一般传输数据大小不超过2k-4k | post请求传输数据的大小根据php.ini 配置文件设定，也可以无限大。 |
| 安全性         | 不安全                         | 安全                                                         |

3.request的相关属性

```
1.请求cookie:request_cookies
```

```
2.创建虚拟环境时:mkvirtualenv spider_3 -p python3
```

```
self.headers = {"User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/75.0.3770.100 Safari/537."}


```

day02

```
1.百度翻译涉及js的加密

```

```
2.xdg-open  1.html
Referer:跳转页面
```

```
3:PyCharm中替换的快捷键是Ctrl+R，选中Regex正则模式
源匹配为 (.*): (.*)$
替换匹配为 "$1": "$2",
```

```
4.贪婪:?非贪婪
```

day03

```
1.GitHub的模拟登陆(day06)
2.MVC(模型视图控制)
3.结构化数据:先有结构后有数据
4.豆瓣请求头中的反扒
5.金色头条代码(下一页的id在前一页的数据中bottom_id中)
```

day04

```python
1.python类快速创建方法
2.ensure_ascii=False ,ident=2
3.多个值传参时没有值得需要赋值为None
报错:unsupported operand type(s) for -: 'dict' and 'int
#return content_list,None,None
#data_dict, next_page_url, page = self.parse_data(data)
```

day05

```
1.并行和并发/超线程技术/多协程猴子补丁
2.线程守护
3.阻塞与非阻塞/同步与异步(举例子)
4.模糊查询:xpath
5.p=Pool()默认个数由cpu决定
  p.apply_async(func)
  apply_async(func[, args[, kwds]]) ：使用非阻塞   方式调用函数，args表示以元组方式给函数传参，kwds表示   以字典方式给函数传参
 6.异步并发阻塞
 7.青蛙跳台阶
 8.jpg是透明的png是透明的
```

day06

```
1.OCR:Optical Character Recognition(光学字符识别)
2.pagerank
3.滚动监听/点击加载下一页/标准带页码的类型/静态翻页类型

1.安装谷歌浏览器
 sudo dpkg -i google-chrome-stable_current_amd64.deb 
 sudo mv chromedriver /usr/local/bin/

```

day07

```
1.Windows下安装redis数据库
creating server tcp listening socket 127.0.0.1:6379: bind No error

的解决方案如下按顺序输入如下命令就可以连接成功如果想要关闭redis服务端并重启方法如下

1. redis-cli.exe
2. shutdown
3. exit
4. redis-server.exe redis.windows.conf

2.参考链接:https://www.cnblogs.com/fjiqiang/p/10549595.html

./mongod --dbpath=/usr/local/data/db
./bin/mongod -dbpath=/data/db


```

```
services.msc查看服务器进程
问题:Windows下安装MongoDB后需要配置环境变量将bin目录路径添加到path
参考链接:https://jingyan.baidu.com/article/86f4a73eba6e9337d7526961.html
```

```
问题:解决Scrapy安装错误:Microsoft Visual C++ 14.0 is required.
参考链接:https://www.cnblogs.com/qxfan/p/8566945.html
问题:10.Python运行Scrapy时出现错误： ModuleNotFoundError: No module named 'win32api'
参考链接:https://github.com/mhammond/pywin32/releases
```

day08

```
1.scrapy crawl itcast命令必须在scrapy.cfg路径下执行
2.自身写的文件在导包时必须要有init文件否则不是导包模块
3.zip()函数拼接两个不同的列表

```

day10

```
1.domzDemo的运行流程
 1.1在pipeline中建立自己的RedisPipeline的管道
 DMOZ网站:Open DirectoryProject
```

day11

```
1.pycharm选中多行快捷键 alt+点击
```

单词

```
dumps :转储       wrapper:包装纸
loads:负载		virtualenv虚拟环境
recursive:递归 	spwan :卵/产卵
sibling:兄弟		deprecated:被弃用
prettify:美化		scrapy:碎屑
XPath: XML Path Language
 selenium:硒		schelduler:调度
 Phantom:幻影    urlib.parse.urljoin()单词
 dummy:假		dupe:欺骗
```


